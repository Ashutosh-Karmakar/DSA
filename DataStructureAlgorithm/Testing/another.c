#include<stdio.h>
#include<stdlib.h>


int findLongestEdge(){
    int visit[20];
    for(int i=0;i<)
}

void addEdges(int adj[][20],int x,int y,int i){
    adj[x][i] = y; 
}

void dfs(){

}

int main(){
    int adj[20][20];
    addEdges(adj, 1, 2,0);
    addEdges(adj, 1, 3,1);
    addEdges(adj, 3, 2,0);
    addEdges(adj, 2, 4,0);
    addEdges(adj, 3, 4,1);
}